import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent calls AppRegistry.registerComponent for you.
// It also ensures that you render the top-level component in the
// Expo Go app as well as in a native stand-alone app.
registerRootComponent(App);
